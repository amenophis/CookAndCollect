{% extends "app/base.html.twig" %}

{% block body %}
    <section class="max-w-6xl mx-auto">
        <h1 class="h1 max-w-3xl mx-auto text-center pb-12">Enregistrer un restaurant</h1>

        <div class="max-w-sm mx-auto">
            {{ form_start(form) }}

            {{ form_row(form.restaurantName) }}

            {{ form_row(form.register) }}

            {{ form_end(form) }}
        </div>
    </section>
{% endblock %}
